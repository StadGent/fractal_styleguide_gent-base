<input type="text"
       name="{{ textfield_name }}"
       id="{{ textfield_id }}"
        {% if modifier %}
          class="{{ modifier }}"
        {% endif %}
        {% if disabled %}
          disabled="disabled"
        {% endif %}
        placeholder="test"
/>
