<input type="number"
       name="{{ number_name }}"
       id="{{ number_id }}"
       {% if modifier %}
         class="{{ modifier }}"
       {% endif %}
      {% if disabled %}
        disabled="disabled"
      {% endif %}
/>
