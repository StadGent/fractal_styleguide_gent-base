<input type="number"
       name="{{ name }}"
       id="{{ id }}"
       {% if modifier %}
         class="{{ modifier }}"
       {% endif %}
      {% if disabled %}
        disabled="disabled"
      {% endif %}
      {% if ariaDescribedBy %}
        aria-describedby="{{ ariaDescribedBy }}"
      {% endif %}
/>
