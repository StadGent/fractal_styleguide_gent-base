<div class="checkbox">
  <input type="checkbox"
        id="{{ checkbox_id }}"
        name="{{ checkbox_name }}"
        class="checkbox {% if modifier %}{{ modifier }}{% endif %}"
  />
  <label for="{{ checkbox_id }}">Checkbox label</label>
</div>
