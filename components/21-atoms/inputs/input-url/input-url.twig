<input type="url"
        name="{{ url_name }}"
        id="{{ url_id }}"
        {% if modifier %}
          class="{{ modifier }}"
        {% endif %}
        {% if disabled %}
          disabled="disabled"
        {% endif %}
/>
