<input type="date"
       name="{{ date_name }}"
       id="{{ date_id }}"
       class="{% if modifier %}{{ modifier }}{% endif %}"
       placeholder="dd/mm/yyyy"
      {% if disabled %}
        disabled="disabled"
      {% endif %}
      pattern="[0-9]{2}/[0-9]{2}/[0-9]{4}"
/>
