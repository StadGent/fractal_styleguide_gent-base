<input type="date"
       name="{{ name }}"
       id="{{ id }}"
       class="{% if modifier %}{{ modifier }}{% endif %}"
       placeholder="dd/mm/yyyy"
      {% if disabled %}
        disabled="disabled"
      {% endif %}
      pattern="[0-9]{2}/[0-9]{2}/[0-9]{4}"
/>
