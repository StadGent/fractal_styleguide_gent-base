<input type="password"
       name="{{ password_name }}"
       id="{{ password_id }}"
       {% if modifier %}
         class="{{ modifier }}"
       {% endif %}
      {% if disabled %}
        disabled="disabled"
      {% endif %}
/>
