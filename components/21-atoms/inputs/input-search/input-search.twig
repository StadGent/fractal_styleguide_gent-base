<input type="search"
       name="{{ search_name }}"
       id="{{ search_id }}"
       placeholder="{{ placeholder }}"
       {% if modifier %}
         class="{{ modifier }}"
       {% endif %}
      {% if disabled %}
        disabled="disabled"
      {% endif %}
      {% if required %}
        required
      {% endif %}
>
