<div class="radio">
  <input type="radio"
        id="{{ id }}"
        name="{{ name }}"
        class="radio {% if modifier %}{{ modifier }}{% endif %}"
        {% if disabled %}
          disabled="disabled"
        {% endif %}
  />
  <label for="{{ id }}">{{ label }}</label>
</div>
