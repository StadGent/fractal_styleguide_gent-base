<input type="email"
       name="{{ email_name }}"
       id="{{ email_id }}"
       {% if modifier %}
         class="{{ modifier }}"
       {% endif %}
      {% if disabled %}
        disabled="disabled"
      {% endif %}
/>

